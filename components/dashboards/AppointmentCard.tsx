import { Badge, Button, Card, CardBody, Heading, HStack, Text } from '@chakra-ui/react'; import { Appointment } from '@/types/appointment'; import { statusVariant } from '@/lib/utils';
export default function AppointmentCard({appointment}:{appointment:Appointment}){return <Card bg="white"><CardBody><HStack justify="space-between"><Badge variant="info">{appointment.program}</Badge><Badge variant={statusVariant(appointment.status)}>{appointment.status}</Badge></HStack><Heading size="md" mt={4}>{appointment.patientName}</Heading><Text color="brand.muted">{appointment.type} with {appointment.providerName}</Text><Text mt={3} fontWeight="800">{appointment.date} · {appointment.time}</Text><HStack mt={4}><Button size="sm" variant="secondary">Reschedule</Button><Button size="sm" variant="ghost">Cancel</Button></HStack></CardBody></Card>}
