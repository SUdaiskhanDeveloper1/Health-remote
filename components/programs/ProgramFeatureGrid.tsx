"use client";
import { Box, Container, SimpleGrid } from '@chakra-ui/react'; import SectionHeading from '@/components/ui/SectionHeading'; import FeatureIconCard from '@/components/ui/FeatureIconCard'; import { Program } from '@/types/program';
export default function ProgramFeatureGrid({program}:{program:Program}){return <Box py={16} bg="white"><Container maxW="7xl"><SectionHeading eyebrow="Features" title={`${program.title} features`} description="Mock UI cards designed to communicate care management capabilities clearly."/><SimpleGrid mt={10} columns={{base:1,md:2,lg:3}} spacing={5}>{program.features.map(f=><FeatureIconCard key={f} title={f} description={`Frontend-only ${f.toLowerCase()} workflow with Chakra cards, badges, and responsive states.`}/>)}</SimpleGrid></Container></Box>}
