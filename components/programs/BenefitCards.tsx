"use client";
import { Box, Container, SimpleGrid } from '@chakra-ui/react'; import SectionHeading from '@/components/ui/SectionHeading'; import FeatureIconCard from '@/components/ui/FeatureIconCard';
export default function BenefitCards(){const b=[['Provider efficiency','Gauge','Reduce visual clutter with organized dashboard sections.'],['Patient engagement','MessageCircle','Show reminders, education, and check-ins.'],['Operational clarity','BarChart3','Surface time, task, and program progress in one UI.']];return <Box py={16} bg="white"><Container maxW="7xl"><SectionHeading eyebrow="Benefits" title="Provider benefits designed into the interface"/><SimpleGrid mt={10} columns={{base:1,md:3}} spacing={5}>{b.map(([t,i,d])=><FeatureIconCard key={t} icon={i} title={t} description={d}/>)}</SimpleGrid></Container></Box>}
