"use client";
import { Badge, Card, CardBody, Heading, HStack, Icon, Stack, Text } from '@chakra-ui/react'; import { iconMap } from '@/lib/iconMap'; import { Device } from '@/types/device'; import { statusVariant } from '@/lib/utils';
export default function DeviceCard({device}:{device:Device}){const I=iconMap[device.icon]||iconMap.Activity;return <Card bg="white" h="full" _hover={{transform:'translateY(-5px)'}}><CardBody><HStack justify="space-between" align="start"><HStack w="46px" h="46px" rounded="16px" bg="brand.blueBg" color="brand.blue" justify="center"><Icon as={I}/></HStack><Badge variant={statusVariant(device.status)}>{device.status}</Badge></HStack><Heading size="md" mt={5}>{device.name}</Heading><Text color="brand.muted" mt={2}>{device.description}</Text><Stack direction="row" wrap="wrap" mt={4}>{device.supportedPrograms.map(p=><Badge variant="info" key={p}>{p}</Badge>)}</Stack><Text mt={4} fontSize="sm" color="brand.muted">Connection: {device.connectionType}</Text></CardBody></Card>}
